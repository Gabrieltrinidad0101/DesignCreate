name: Build and Deploy

on:
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: |
          docker login -u gabrielopensource -p ${{ secrets.PACKAGE_TOKEN }} ghcr.io
          docker compose -f ./backend/docker-compose.yml build designcreate_pro
          docker push ghcr.io/Gabrieltrinidad0101/designcreate_pro:latest
